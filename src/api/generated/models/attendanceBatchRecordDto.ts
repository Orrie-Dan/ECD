/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ECD Backend API
 * Early Childhood Development management system API. Success responses are bare DTO bodies; errors use `{ success, statusCode, message, timestamp }` (plus `entity` / `currentVersion` on optimistic-lock conflicts). List endpoints use offset pagination (`items`, `page`, `pageSize`, `total`, `totalPages`); sync pull uses cursor pagination.
 * OpenAPI spec version: 1.0
 */
import type { AbsentReason } from './absentReason';

export interface AttendanceBatchRecordDto {
  childId: string;
  /** Attendance date (YYYY-MM-DD) */
  date: string;
  /** Whether the child was present */
  present: boolean;
  absentReason?: AbsentReason;
  /** @maxLength 1000 */
  notes?: string;
  /**
   * Required when correcting an existing attendance row (optimistic lock). Omit on first create.
   * @minimum 0
   */
  version?: number;
  /** Originating device UUID for offline sync */
  deviceId?: string;
  /**
   * Client-side local identifier for sync correlation
   * @maxLength 100
   */
  localId?: string;
}
