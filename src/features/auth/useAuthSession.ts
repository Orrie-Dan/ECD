/**
 * @deprecated Import from `@/features/auth` (queries / mutations) instead.
 * Kept as a thin re-export for any residual deep imports.
 */
export { useCurrentUser } from './queries'
export { useLogin, useLogout, type LoginResult, type LoginError } from './mutations'
